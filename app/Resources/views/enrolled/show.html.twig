{% extends 'base.html.twig' %}

{% block body %}
    {% if  is_granted('ROLE_USER') %}

            <p class="text-center">Naozaj sa chcete odhlásiť z kurzu <br> <b>{{ enrolled.courseInstance.courseType }}</b>?</p>
            <div class="text-center">
        {{ form_start(delete_form) }}
            <input type="submit" value="Odhlásiť sa">
            {{ form_end(delete_form) }}

            </div>



    <ul>
        <li>
            <a href="{{ path('coursetype_index') }}">Naspäť na kurzy</a>
        </li>

    </ul>
{% endif %}
{% endblock %}
